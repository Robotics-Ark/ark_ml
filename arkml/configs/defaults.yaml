defaults:
  - algo: pizero
  - data: pizero_dataset

# general overrides
seed: 42
output_dir: ./outputs
checkpoint: None
task_prompt: ""
n_episodes: 1
max_steps: 500
step_sleep: 0.1

# Policy Config
stepper_frequency: 5

# Pub-Sub
# Default channel config file used by PolicyNode. Override via CLI if needed.
channel_config: "examples/franka_pick_place/policy_channels.yaml"

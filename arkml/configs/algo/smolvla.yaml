name: smolvla
model:
  type: VLA
  name: VLAHubPolicy
  import_path: lerobot.common.policies.smolvla.modeling_smolvla
  class_name: SmolVLAPolicy
  pretrained_model: lerobot/smolvla_base
  obs_dim: 10
  action_dim: 8
  obs_horizon: 8
  pred_horizon: 16
  action_horizon: 8

trainer:
  device: cuda
